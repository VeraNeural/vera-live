# Copy to .env.local (recommended) or .env and fill values.
# Never commit real secrets.

# ============================================================================
# AI PROVIDERS
# ============================================================================

# Hume (server-side)
# Preferred name for narration/TTS:
HUMEAI_API_KEY=
# Back-compat names accepted by /api/narrate:
# HUME_API_KEY=

# ============================================================================
# DATABASE
# ============================================================================

# Supabase (server-side admin + public URL)
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# ============================================================================
# SECURITY & ENCRYPTION (HIPAA-aligned)
# ============================================================================

# Field-level encryption key for sensitive data (32 bytes, hex-encoded)
# Generate with: openssl rand -hex 32
# ENCRYPTION_KEY=

# Salt for audit log hashing (optional, defaults to internal value)
# AUDIT_HASH_SALT=

# API key for external cron to trigger audit log cleanup
# ADMIN_CLEANUP_API_KEY=

# Audit log retention period in days (default: 365)
# AUDIT_RETENTION_DAYS=365

# ============================================================================
# APPLICATION
# ============================================================================

# Optional
NEXT_PUBLIC_APP_URL=http://localhost:3000

ADMIN_EMAIL=support@veraneural.com